<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="16dp">

    <!-- Welcome Text -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Welcome"
        android:textSize="30sp"
        android:textAlignment="center"
        android:layout_marginBottom="40dp" />

    <!-- Button to Proceed to Login/Signup Page -->
    <Button
        android:id="@+id/btnProceed"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Click To Enter Login And Signup Page"
        android:textSize="18sp"
        android:padding="20dp" />

    <!-- Button to Proceed to Difficulty Page -->
    <Button
        android:id="@+id/btnDifSet"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Click To Enter Difficulty Settings"
        android:textSize="18sp"
        android:padding="20dp" />

    <!-- Button to pick character -->
    <Button
        android:id="@+id/charSelectionBtn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Character Selection"
        android:textSize="18sp"
        android:padding="20dp"/>

    <!-- Button to game screen -->
    <Button
        android:id="@+id/levelBtn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Game Screen"
        android:textSize="18sp"
        android:padding="20dp"/>

</LinearLayout>

